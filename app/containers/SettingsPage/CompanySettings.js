import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const Container = styled.div`
  text-align: center;
`;

const Content = (props) => <Container>Company</Container>

export default Content;
