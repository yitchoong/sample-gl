import React from 'react';
import PropTypes from 'prop-types';

import styled from 'styled-components';

const Container = styled.div`
  text-align: center;
`;

const Content = (props) => <Container>GL Period</Container>

export default Content;
